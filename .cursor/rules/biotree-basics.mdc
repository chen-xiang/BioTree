---
description: BioTree 基础开发规范（文件头、注释日志、测试、设计、UI、变更策略）
alwaysApply: true
---

# BioTree 基础 Rule

编写或修改本仓库任何代码时必须遵守以下规范。完整说明见 `docs/CODING_RULES.md`。

## 文件头

- 每个新建源码文件必须有完整文件头：简述 + `Author: chen-xiang` + `Created: YYYY-MM-DD`。
- 新建只写 `Created`，不写 `Updated`。
- 每次实质修改在文件头下方追加：`Updated: YYYY-MM-DD` + 一句中文说明。
- 纯格式化、无损重命名可不追加 `Updated`。

## 语言

- 代码注释用**简体中文**。
- 日志文案用**英文**。
- 用户可见 UI 文案走 vue-i18n，不硬编码单一语言。

## 测试

- 需要完整测试用例。
- 实现变更时同步补充或修复测试，禁止只改实现不改测试。

## 设计

- 高内聚、低耦合。
- 少魔鬼数字（用常量/枚举）。
- 信任域内不过度防御；严格校验放在系统边界。

## 前端 UI

- 响应式；支持深浅模式与多语言。
- 交互与视觉一致；尽量复用设计系统组件。
- Token 化、组件化；界面美观精致；关键交互要有动效。

## 变更策略

- 默认不保留旧实现。
- 不做 deprecated 过渡；直接替换，不留双轨兼容代码。
